name: Build Android ARM64

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # تنظیم جاوا ۱۷ (ارتقا به v5 برای جلوگیری از خطای منسوخ شدن)
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      # نصب و تنظیم خودکار گریدل ۸.۴ (بدون نیاز به gradlew در ریپازیتوری)
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.4'

      # اجرای مستقیم دستور gradle
      - name: Build release APK for ARM64
        run: |
          gradle clean assembleRelease

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: kochakdns-arm64-apk
          path: app/build/outputs/apk/release/app-release.apk
